<!--
  @fileoverview DataTable component that displays tabular data with customizable headers and items.
  @component DataTable
  @author Eva Ray
  @description This component renders a responsive table based on the provided headers and items. It allows for
  flexible data display and can be reused for different datasets. The style has been generated by an LLM.
 -->

<script setup>
defineProps({
  headers: {
    type: Array,
    required: true
  },
  items: {
    type: Array,
    required: true
  },
  keyField: {
    type: String,
    default: 'id'
  }
})
</script>

<template>
  <div class="table-responsive">
    <table class="results-table">
      <thead>
      <tr>
        <th v-for="header in headers" :key="header.key" :class="header.headerClass">
          {{ header.label }}
        </th>
      </tr>
      </thead>
      <tbody>
      <tr v-for="(item, index) in items" :key="item[keyField] || index">
        <td v-for="header in headers" :key="header.key" :class="header.cellClass">
          {{ item[header.key] ?? '—' }}
        </td>
      </tr>
      </tbody>
    </table>
  </div>
</template>